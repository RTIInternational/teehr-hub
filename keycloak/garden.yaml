kind: Deploy
type: kubernetes
name: keycloak
description: Keycloak instance managed by Keycloak Operator
dependencies:
  - deploy.secrets
  - deploy.keycloak-database
  - deploy.keycloak-operator
environments:
  - local
spec:
  manifestTemplates:
    - ./manifests/keycloak.yaml.tpl
