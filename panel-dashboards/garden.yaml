kind: Build
type: container
name: panel-dashboards-image
description: Build TEEHR Panel Dashboards image
spec:
  dockerfile: Dockerfile
  publishId: panel-dashboards:${var.devTeehrVersion}
  localId: panel-dashboards
  buildArgs:
      BASE_IMAGE: ${actions.build.teehr-jupyter-driver-image-edge.outputs.deploymentImageId}
environments:
  - local
  - remote
dependencies:
  - build.teehr-jupyter-driver-image-edge
---
kind: Deploy
type: kubernetes
name: panel-app
spec:
  manifestTemplates:
    - manifests/panel-app.yaml.tpl
dependencies:
  - build.panel-dashboards-image
environments:
  - local
  - remote