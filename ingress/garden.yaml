kind: Deploy
type: kubernetes
name: jupyterhub-ingress
spec:
  manifestTemplates:
    - manifests/hub.yaml.tpl
environments:
  - local
  - remote
---
kind: Deploy
type: kubernetes
name: minio-ingress
spec:
  manifestTemplates:
    - manifests/minio.yaml.tpl
environments:
  - local
---
kind: Deploy
type: kubernetes
name: panel-ingress
spec:
  manifestTemplates:
    - manifests/panel.yaml.tpl
environments:
  - local
  - remote
---
kind: Deploy
type: kubernetes
name: api-ingress
spec:
  manifestTemplates:
    - manifests/api.yaml.tpl
environments:
  - local
  - remote
---
kind: Deploy
type: kubernetes
name: dashboards-ingress
spec:
  manifestTemplates:
    - manifests/dashboards.yaml.tpl
environments:
  - local
  - remote
---
kind: Deploy
type: kubernetes
name: auth-ingress
spec:
  manifestTemplates:
    - manifests/auth.yaml.tpl
dependencies:
  - deploy.keycloak-bootstrap
environments:
  - local
  - remote
---
kind: Deploy
type: kubernetes
name: prefect-ingress
spec:
  manifestTemplates:
    - manifests/prefect.yaml.tpl
dependencies:
  - deploy.prefect-oidc-proxy
environments:
  - local
  - remote
# ---
# kind: Deploy
# type: kubernetes
# name: grafana-ingress
# spec:
#   manifestTemplates:
#     - manifests/grafana.yaml.tpl
# environments:
#   - remote