kind: Deploy
type: kubernetes
name: spark-roles
environments:
  - local
  - remote
spec:
  files:
    # - manifests/spark-namespace.yaml
    - manifests/spark-roles.yaml
    - manifests/jupyter-spark-rbac.yaml