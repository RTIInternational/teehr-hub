kind: Deploy
type: kubernetes
name: spark-roles
environments:
  - local
  - remote
spec:
  manifestTemplates:
    # - manifests/spark-namespace.yaml.tpl
    - manifests/spark-roles.yaml.tpl
    - manifests/jupyter-spark-rbac.yaml.tpl
    - manifests/prefect-spark-rbac.yaml.tpl