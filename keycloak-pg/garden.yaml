kind: Deploy
type: kubernetes
name: keycloak-database
description: K8s Deploy Keycloak Database
dependencies:
  - deploy.secrets
environments:
  - local
spec:
  manifestFiles:
  - ./manifests/keycloak-pg.yaml
