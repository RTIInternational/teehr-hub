kind: Deploy
type: exec
name: keycloak-operator
description: Install Keycloak Operator directly with kubectl (without OLM)
environments:
  - local
  - remote
spec:
  deployCommand:
    - bash
    - -c
    - >-
      kubectl apply -f https://raw.githubusercontent.com/keycloak/keycloak-k8s-resources/26.6.1/kubernetes/keycloaks.k8s.keycloak.org-v1.yml &&
      kubectl apply -f https://raw.githubusercontent.com/keycloak/keycloak-k8s-resources/26.6.1/kubernetes/keycloakrealmimports.k8s.keycloak.org-v1.yml &&
      kubectl create namespace "${environment.namespace}" --dry-run=client -o yaml | kubectl apply -f - &&
      kubectl -n "${environment.namespace}" delete deployment keycloak-operator --ignore-not-found=true &&
      kubectl -n "${environment.namespace}" apply -f https://raw.githubusercontent.com/keycloak/keycloak-k8s-resources/26.6.1/kubernetes/kubernetes.yml &&
      kubectl patch clusterrolebinding keycloak-operator-clusterrole-binding --type='json' -p='[{"op":"replace","path":"/subjects/0/namespace","value":"${environment.namespace}"}]' &&
      kubectl -n "${environment.namespace}" rollout status deployment/keycloak-operator --timeout=180s
