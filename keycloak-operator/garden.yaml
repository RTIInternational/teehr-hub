kind: Deploy
type: kubernetes
name: keycloak-operator
description: Install Official Keycloak Operator via OLM subscription manifests
dependencies:
  - deploy.olm
environments:
  - local
  - remote
spec:
  manifestTemplates:
    - ./manifests/operatorgroup.yaml.tpl
  manifestFiles:
    - ./manifests/subscription.yaml
