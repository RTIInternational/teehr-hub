kind: Deploy
type: kubernetes
name: minio-deploy
description: K8s Deploy MinIO
spec:
  files: ["./manifests/minio.yaml"]
dependencies:
  - deploy.secrets
environments:
  - local
---
kind: Deploy
type: kubernetes
name: create-minio-bucket
description: K8s Job Create MinIO Bucket
spec:
  files: ["./manifests/create-bucket.yaml"]
dependencies:
  - deploy.secrets
  - deploy.minio-deploy
environments:
  - local